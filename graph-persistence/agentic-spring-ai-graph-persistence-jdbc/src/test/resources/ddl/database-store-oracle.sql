CREATE TABLE spring_ai_store (pk_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, id VARCHAR2(4000) NOT NULL, id_hash CHAR(64) NOT NULL UNIQUE, namespace VARCHAR2(4000), key_name VARCHAR2(500), value_json CLOB, created_at TIMESTAMP, updated_at TIMESTAMP)
